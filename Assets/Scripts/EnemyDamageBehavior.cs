using System;
using UnityEngine;

namespace Assets.Scripts
{
    public class EnemyDamageBehavior : MonoBehaviour
    {
        private void Damage()
        {
            Mathf.Atan2(0, 0);
            Math.Atan2(0, 0);
            Destroy(gameObject);
        }
    }
}